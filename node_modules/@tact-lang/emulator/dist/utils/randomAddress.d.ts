import { Address, ExternalAddress } from "ton-core";
export declare function randomAddress(seed: string, workchain?: number): Address;
export declare function randomExternalAddress(seed: string): ExternalAddress;
